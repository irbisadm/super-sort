# @semantic-release/npm@13 (needed for OIDC trusted publishing) declares
# Node >=22.14 in its engines. The release job runs on Node 22, but local dev
# and the CI build/test job run on Node 20, where it is only installed, never
# executed. Skip the engine check so those installs don't hard-fail.
ignore-engines true
